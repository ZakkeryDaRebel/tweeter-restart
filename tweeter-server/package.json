{
  "name": "tweeter-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run compile",
    "compile": "npm run clean && tsc",
    "clean": "rm -rf dist",
    "copy-shared": "rm -rf node_modules/tweeter-shared && cp -R ../tweeter-shared node_modules/tweeter-shared && rm -rf layer/nodejs/node_modules/tweeter-shared && cp -R ../tweeter-shared layer/nodejs/node_modules/tweeter-shared"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "tweeter-shared": "file:../tweeter-shared",
    "date-fns": "^3.3.1",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.2"
  }
}
